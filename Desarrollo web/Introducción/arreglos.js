const arreglo = ['texto', 456.5, false, {propiedad: 'valor'}, [1, 2, 3]];
console.log(arreglo)

const amigos = ['Alejandro', 'Thiago', 'Sebas'];
console.log(amigos[0]);

const colores = [];
colores[0] = 'rojo';
colores[2] = 'verde';
colores[3] = 'blanco';
console.log(colores);

console.log(colores.length);

colores.push('Negro');

console.log(colores);

console.log(colores.length);

